# mysql 실행
#mysql 이미지를 가지고 mysqlcon 컨테이너 생성 포트 3306 detach실행
docker run -d -p 3306:3306 --name mysqlcon -e MYSQL_ROOT_PASSWORD=1234 mysql    //터미널 열어서 이거 shift + insert
#실제 윈도우 3306 포트로 실행
#가상 OS리눅스 3306