내컴퓨터
127.0.0.1
localhost

ipconfig
192.168.0.29

쌤자리ip
192.168.0.242

재형님ip
192.168.0.35

내ip
192.168.0.80

docker프로그램
dockerhub desktop

#nginx이미지를 가지고 mynginx 컨테이너 생성 포트 80:80 detach 실행 os = 리눅스

docker run -d -p 3306:3306 --name mysqlcon mysql

현재폴더에 있는 모든 파일 복사
docker cp . mynginx:usr/share/nginx/html

nginx이미지를 가지고 mynginx 컨테이너 생성 포트 80:80 detach 실행
docker run -d -p 80:80 --name mynginx nginx

