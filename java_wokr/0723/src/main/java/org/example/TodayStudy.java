package org.example;
/*

interface -> 몸체가 없는 메서드를 정의할 수 있음
void doA()만 적고 public 안 적어도 됨
변수도 int A 적고 public static final 안 적어도 됨

class 만들어서 interface 상속 받아야 함
doA() 메서드 재정의 해야함

default() static() 은 추가 가능 but 앞에 아무 것도 안 붙은 건(void doA() 같은 거) 하나만 있어야 함
메서드 안에 내용 존재할 수 있음

default() -> new~로 재정의 한 후 참조 가능
static() -> Printable.doA();로 참조 가능

람다 규칙
interface 안에 하나의 추상메서드(바디내용없는거)가 존재해야한다.
() -> {}

docker desktop
데이터베이스 종류 많음....-> 우효림 선생님과 공부하자

MYSQL 서버설치
docker run 명령어로 설치(가상 OS)
heidsql(클라이언트)로 도커 서버 접속함

java언어로도 접속할 거임 -> try-catch구문으로

18장..
try{} catch{}예외상황에 대해서 공부중~

 */
public class TodayStudy {
}
